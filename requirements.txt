# torch: if you don't have an NVIDIA GPU, install via the CPU wheel index
# instead of this line -- see README "Setup" section. Installing the
# default PyPI wheel with `pip install torch` pulls ~5GB of CUDA libraries
# even for CPU-only use.
torch>=2.2
pennylane>=0.38
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
yfinance>=0.2
mlflow>=2.15
matplotlib>=3.8
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
psycopg2-binary>=2.9
httpx>=0.27
requests>=2.31
pytest>=8.0
ruff>=0.6
nbformat>=5.10
